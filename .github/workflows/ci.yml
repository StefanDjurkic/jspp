name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  compiler:
    name: Build compiler (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Configure CMake
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build jspp
        run: cmake --build build --config Release --target jspp

      - name: Upload jspp binary
        uses: actions/upload-artifact@v4
        with:
          name: jspp-${{ matrix.os }}
          path: |
            build/jspp
            build/Release/jspp.exe
          if-no-files-found: ignore

  tests:
    name: Regression tests (Node ${{ matrix.node }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: ['20', '22']
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}

      - name: Run regression test suite
        run: node tests/run.mjs

  examples:
    name: Run examples
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Execute every example with the interpreter
        shell: bash
        run: |
          set -e
          # The Node reference interpreter is a subset of the full language.
          # These examples exercise features only the C++ compiler currently
          # supports (generics, nested type declarations inside classes, cpp
          # blocks used as expressions) and are skipped here.
          skip=("examples/generics.jspp" "examples/game_entity.jspp" "examples/cpp_interop.jspp")
          is_skipped() {
            for s in "${skip[@]}"; do [ "$1" = "$s" ] && return 0; done
            return 1
          }
          failures=0
          for f in examples/*.jspp; do
            if is_skipped "$f"; then
              echo "::notice file=$f::skipped (interpreter-unsupported feature)"
              continue
            fi
            echo "::group::$f"
            if ! node prototype/jspp.mjs "$f" --run; then
              echo "::error file=$f::example failed"
              failures=$((failures + 1))
            fi
            echo "::endgroup::"
          done
          if [ "$failures" -ne 0 ]; then
            echo "$failures example(s) failed"
            exit 1
          fi
